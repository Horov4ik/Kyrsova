{
  "name": "kursa-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd client && npm ci && npm run build",
    "start": "cd server && npm run start",
    "postinstall": "cd client && npm ci && npm run build || true"
  }
}
